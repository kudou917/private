#include <stdio.h>
    //整数値を入力させ、その値が5よりも大きく、
    //かつ、20よりも小さければOKと表示するプログラムを作成せよ。
    int main (void){
        int a = 0;
       printf("input number:");
        scanf("%d", &a);
       if (a <= -5) {
        ; 
    }

    else if (a >= 10) {
   }   

    else  {
   printf("OK"); 
    }   
    return 0;
    }