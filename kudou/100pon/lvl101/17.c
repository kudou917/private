#include <stdio.h>
//整数値を入力させ、0から入力値まで数を1ずつ増やして表示するプログラムを作成せよ。
int main (void) {
    int i;
    int num[10];
    for ( i = 0; i < 10; i++)
    {
    printf("%d\n",  num[i] = i );
    }
    return 0;
}